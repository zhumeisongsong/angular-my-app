export class Order {
  userId: number;
  orderState: number=0;
  // consignee: string;
  // province: number;
  // city: number;
  // district: number;
  // address: string;
  // mobile: string;
  totalPrice: number;
  totalCount:number;
  userNote?: string;
  orderId?: number;
}
