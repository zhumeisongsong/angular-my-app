export class Order {
  userId: number=0;
  orderState: number=0;
  // consignee: string;
  // province: number;
  // city: number;
  // district: number;
  // address: string;
  // mobile: string;
  totalPrice: number=0;
  totalCount:number=0;
  userNote?: string;
  id?: number;
}
