//order center feature module的壳 没有也不需要指定selector 只有用于放置子视图的<router-outlet>
import{Component} from '@angular/core';

@Component({
  template: `<router-outlet></router-outlet>`
})

export class OrderComponent {

}
